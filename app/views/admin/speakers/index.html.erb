<%- model_class = Admin::Speaker -%>
<%- provide(:title, t('.title')) -%>
<%- provide(:navigation_highlight, 'tools') -%>

<div class="row">
  <div class="large-12 columns">
    <%= new_button(:admin, :speaker, :zurb) %>
    <%= render 'shared/filter' %>
    <%= render 'search' %>
    <%= will_paginate(@admin_speakers) %>

    <table>
      <thead>
        <tr>
          <th><%= model_class.human_attribute_name(:name) %></th>
          <th><%= model_class.human_attribute_name(:created_at) %></th>
          <th><%=t '.actions', default: t('helpers.actions') %></th>
        </tr>
      </thead>
      <tbody>
        <% @admin_speakers.each do |speaker| %>
          <tr>
            <td><%= link_to speaker, admin_speaker_path(speaker) %></td>
            <td><%=l speaker.created_at, format: :created_at_date %></td>
            <td>
              <%= mini_edit_button(:admin, :speaker, speaker, :zurb) %>
              <%= mini_destroy_button(:admin, :speaker, speaker, :zurb) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
